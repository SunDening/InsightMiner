
KB 知识库描述长期优化方案
=========================

当前状态：方案 A（启动时 LLM 摘要，mtime 缓存）
  位置：build_kb_descriptions() → classify_intent_node 引用 _kb_descriptions


方案 B：ChromaDB 级动态感知
--------------------------
状态：未实施

思路：
  - 不从文件名生成摘要，改为从 ChromaDB collection 的 metadata 中提取文档来源
  - 这反映的是实际已索引的内容，与索引状态完全一致
  - 支持运行时上传文档后自动感知（因为上传后必定建索引）

实施时机：
  - 实现文档上传功能时一起做
  - 需要一个文档 metadata 持久化机制（文件名→文档名/描述 的映射表）


方案 C：缓存 + 失效通知
--------------------------
状态：未实施

思路：
  - 启动时提取一次摘要，缓存到全局变量
  - 文档变更时通过钩子/回调刷新缓存（如 FastAPI endpoint 触发的上传/删除事件）
  - 每次分类零额外开销

实施时机：
  - 做上传功能时，在上传/删除 API 中加一行缓存失效调用即可


n. 知识库中文档过多或文档中字数太多时，为每个文档生成摘要，把摘要向量化。
   在检索时，先通过匹配摘要找到证据可能存在的文档（尽可能多召回），
   之后临时向量化相应文档内容并做匹配。


知识库不同语言导致向量化后难以根据语义匹配。
如果要支持多语言，可能需要更强大的嵌入模型。


Schema 列级语义富化（可选优化）
==============================

当前状态：仅表级富化（P0），已实施。列级使用 schema.json 原始描述。

待实施内容：
  - 对每张表的核心列（PK、FK、高频查询列）调 LLM 生成业务视角描述
  - 包含：业务含义、典型值示例、查询使用方式（WHERE/GROUP BY/JOIN 等）
  - 预估成本：64 表 × ~7 列/表 ≈ 450 次 LLM 调用
  - 富化后的列描述会比原始"通知主管部门的国家代码"式描述更利于语义检索

实施时机：
  - 用户反馈表级富化不够、列级检索不准时
  - 或预算充足时作为一次性离线任务

代码位置：
  - schema_enricher.py: enrich_all() — 已预留列级富化代码结构
  - 取消注释第 155-176 行的列级 LLM 调用即可启用